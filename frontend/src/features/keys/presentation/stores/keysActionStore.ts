/**
 * KeysActionStore — auto-generated by .tmp_gen_stores.mjs.
 *
 * Thin store wrapping the Action-side Repository singletons for this feature.
 * Every action delegates to `repo.<method>(...)` inside a try/finally that
 * toggles per-method loading/error state. Follows the §5.4 R5.1 double-export
 * pattern so tests can inject a mock repo via `createKeysActionStore(mock)`.
 *
 * When a feature needs custom orchestration (multi-repo transactions,
 * derived state, cache invalidation) — replace this generated file with a
 * hand-written store; the generator will skip files it did not create.
 */

import { defineStore } from 'pinia'
import { reactive } from 'vue'
import type { KeysRepository } from '@/features/keys/domain/repositories/keysRepository'
import { keysRepository as default_keys_repo } from '@/features/keys/data/repositories/keysRepositoryImpl'

export function createKeysActionStore(keysRepo: KeysRepository = default_keys_repo) {
  return defineStore('keys/action', () => {
    const loading = reactive<Record<string, boolean>>({ create: false, update: false, deleteKey: false, toggleStatus: false })
    const errors = reactive<Record<string, unknown>>({ create: null as unknown, update: null as unknown, deleteKey: null as unknown, toggleStatus: null as unknown })

  const create: KeysRepository['create'] = ((...args: unknown[]) => {
    loading.create = true
    errors.create = null
    return Promise.resolve()
      .then(() => (keysRepo.create as (...a: unknown[]) => unknown)(...args))
      .catch((error: unknown) => { errors.create = error; throw error })
      .finally(() => { loading.create = false })
  }) as KeysRepository['create']

  const update: KeysRepository['update'] = ((...args: unknown[]) => {
    loading.update = true
    errors.update = null
    return Promise.resolve()
      .then(() => (keysRepo.update as (...a: unknown[]) => unknown)(...args))
      .catch((error: unknown) => { errors.update = error; throw error })
      .finally(() => { loading.update = false })
  }) as KeysRepository['update']

  const deleteKey: KeysRepository['deleteKey'] = ((...args: unknown[]) => {
    loading.deleteKey = true
    errors.deleteKey = null
    return Promise.resolve()
      .then(() => (keysRepo.deleteKey as (...a: unknown[]) => unknown)(...args))
      .catch((error: unknown) => { errors.deleteKey = error; throw error })
      .finally(() => { loading.deleteKey = false })
  }) as KeysRepository['deleteKey']

  const toggleStatus: KeysRepository['toggleStatus'] = ((...args: unknown[]) => {
    loading.toggleStatus = true
    errors.toggleStatus = null
    return Promise.resolve()
      .then(() => (keysRepo.toggleStatus as (...a: unknown[]) => unknown)(...args))
      .catch((error: unknown) => { errors.toggleStatus = error; throw error })
      .finally(() => { loading.toggleStatus = false })
  }) as KeysRepository['toggleStatus']

    return { loading, errors, create, update, deleteKey, toggleStatus }
  })
}

export const useKeysActionStore = createKeysActionStore()
