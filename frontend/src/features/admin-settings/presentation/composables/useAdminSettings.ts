/* Auto-generated by .tmp_gen_composables.mjs */
/**
 * useAdminSettings — thin orchestrator composable for AdminSettings.
 * Re-exports all Query + Action store state and methods as a flat interface.
 * Per spec §5.5 R6: composable coordinates stores + lifecycle.
 * See usePromptAudit.ts for the hand-crafted reference implementation with
 * full i18n error mapping and UI-local state — this composable is the
 * minimal scaffold for features that do not yet have hand-crafted logic.
 */

import { useAdminSettingsQueryStore } from '@/features/admin-settings/presentation/stores/adminSettingsQueryStore'
import { useAdminSettingsActionStore } from '@/features/admin-settings/presentation/stores/adminSettingsActionStore'

export function useAdminSettings() {
  const q_adminSettingsQueryStore = useAdminSettingsQueryStore()
  const a_adminSettingsActionStore = useAdminSettingsActionStore()
  return {
    ...q_adminSettingsQueryStore,
    ...a_adminSettingsActionStore,
  }
}
