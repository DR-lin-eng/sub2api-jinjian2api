/**
 * AnnouncementsQueryStore — auto-generated by .tmp_gen_stores.mjs.
 *
 * Thin store wrapping the Query-side Repository singletons for this feature.
 * Every action delegates to `repo.<method>(...)` inside a try/finally that
 * toggles per-method loading/error state. Follows the §5.4 R5.1 double-export
 * pattern so tests can inject a mock repo via `createAnnouncementsQueryStore(mock)`.
 *
 * When a feature needs custom orchestration (multi-repo transactions,
 * derived state, cache invalidation) — replace this generated file with a
 * hand-written store; the generator will skip files it did not create.
 */

import { defineStore } from 'pinia'
import { reactive } from 'vue'
import type { AdminAnnouncementsRepository } from '@/features/announcements/domain/repositories/adminAnnouncementsRepository'
import type { AnnouncementsRepository } from '@/features/announcements/domain/repositories/announcementsRepository'
import { adminAnnouncementsRepository as default_adminAnnouncements_repo } from '@/features/announcements/data/repositories/adminAnnouncementsRepositoryImpl'
import { announcementsRepository as default_announcements_repo } from '@/features/announcements/data/repositories/announcementsRepositoryImpl'

export function createAnnouncementsQueryStore(adminAnnouncementsRepo: AdminAnnouncementsRepository = default_adminAnnouncements_repo, announcementsRepo: AnnouncementsRepository = default_announcements_repo) {
  return defineStore('announcements/query', () => {
    const loading = reactive<Record<string, boolean>>({ list: false, getById: false, getReadStatus: false, announcements_list: false })
    const errors = reactive<Record<string, unknown>>({ list: null as unknown, getById: null as unknown, getReadStatus: null as unknown, announcements_list: null as unknown })

  const list: AdminAnnouncementsRepository['list'] = ((...args: unknown[]) => {
    loading.list = true
    errors.list = null
    return Promise.resolve()
      .then(() => (adminAnnouncementsRepo.list as (...a: unknown[]) => unknown)(...args))
      .catch((error: unknown) => { errors.list = error; throw error })
      .finally(() => { loading.list = false })
  }) as AdminAnnouncementsRepository['list']

  const getById: AdminAnnouncementsRepository['getById'] = ((...args: unknown[]) => {
    loading.getById = true
    errors.getById = null
    return Promise.resolve()
      .then(() => (adminAnnouncementsRepo.getById as (...a: unknown[]) => unknown)(...args))
      .catch((error: unknown) => { errors.getById = error; throw error })
      .finally(() => { loading.getById = false })
  }) as AdminAnnouncementsRepository['getById']

  const getReadStatus: AdminAnnouncementsRepository['getReadStatus'] = ((...args: unknown[]) => {
    loading.getReadStatus = true
    errors.getReadStatus = null
    return Promise.resolve()
      .then(() => (adminAnnouncementsRepo.getReadStatus as (...a: unknown[]) => unknown)(...args))
      .catch((error: unknown) => { errors.getReadStatus = error; throw error })
      .finally(() => { loading.getReadStatus = false })
  }) as AdminAnnouncementsRepository['getReadStatus']

  const announcements_list: AnnouncementsRepository['list'] = ((...args: unknown[]) => {
    loading.announcements_list = true
    errors.announcements_list = null
    return Promise.resolve()
      .then(() => (announcementsRepo.list as (...a: unknown[]) => unknown)(...args))
      .catch((error: unknown) => { errors.announcements_list = error; throw error })
      .finally(() => { loading.announcements_list = false })
  }) as AnnouncementsRepository['list']

    return { loading, errors, list, getById, getReadStatus, announcements_list }
  })
}

export const useAnnouncementsQueryStore = createAnnouncementsQueryStore()
