/* Auto-generated by .tmp_gen_composables.mjs */
/**
 * useAdminBackup — thin orchestrator composable for AdminBackup.
 * Re-exports all Query + Action store state and methods as a flat interface.
 * Per spec §5.5 R6: composable coordinates stores + lifecycle.
 * See usePromptAudit.ts for the hand-crafted reference implementation with
 * full i18n error mapping and UI-local state — this composable is the
 * minimal scaffold for features that do not yet have hand-crafted logic.
 */

import { useAdminBackupQueryStore } from '@/features/admin-backup/presentation/stores/adminBackupQueryStore'
import { useAdminBackupActionStore } from '@/features/admin-backup/presentation/stores/adminBackupActionStore'

export function useAdminBackup() {
  const q_adminBackupQueryStore = useAdminBackupQueryStore()
  const a_adminBackupActionStore = useAdminBackupActionStore()
  return {
    ...q_adminBackupQueryStore,
    ...a_adminBackupActionStore,
  }
}
