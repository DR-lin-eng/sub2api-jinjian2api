/* Auto-generated by .tmp_gen_composables.mjs */
/**
 * useAdminChannels — thin orchestrator composable for AdminChannels.
 * Re-exports all Query + Action store state and methods as a flat interface.
 * Per spec §5.5 R6: composable coordinates stores + lifecycle.
 * See usePromptAudit.ts for the hand-crafted reference implementation with
 * full i18n error mapping and UI-local state — this composable is the
 * minimal scaffold for features that do not yet have hand-crafted logic.
 */

import { useAdminChannelsQueryStore } from '@/features/admin-channels/presentation/stores/adminChannelsQueryStore'
import { useAdminChannelsActionStore } from '@/features/admin-channels/presentation/stores/adminChannelsActionStore'

export function useAdminChannels() {
  const q_adminChannelsQueryStore = useAdminChannelsQueryStore()
  const a_adminChannelsActionStore = useAdminChannelsActionStore()
  return {
    ...q_adminChannelsQueryStore,
    ...a_adminChannelsActionStore,
  }
}
