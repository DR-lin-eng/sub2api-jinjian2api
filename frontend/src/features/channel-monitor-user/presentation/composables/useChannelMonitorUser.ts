/* Auto-generated by .tmp_gen_composables.mjs */
/**
 * useChannelMonitorUser — thin orchestrator composable for ChannelMonitorUser.
 * Re-exports all Query + Action store state and methods as a flat interface.
 * Per spec §5.5 R6: composable coordinates stores + lifecycle.
 * See usePromptAudit.ts for the hand-crafted reference implementation with
 * full i18n error mapping and UI-local state — this composable is the
 * minimal scaffold for features that do not yet have hand-crafted logic.
 */

import { useChannelMonitorUserQueryStore } from '@/features/channel-monitor-user/presentation/stores/channelMonitorUserQueryStore'
import { useChannelMonitorUserActionStore } from '@/features/channel-monitor-user/presentation/stores/channelMonitorUserActionStore'

export function useChannelMonitorUser() {
  const q_channelMonitorUserQueryStore = useChannelMonitorUserQueryStore()
  const a_channelMonitorUserActionStore = useChannelMonitorUserActionStore()
  return {
    ...q_channelMonitorUserQueryStore,
    ...a_channelMonitorUserActionStore,
  }
}
