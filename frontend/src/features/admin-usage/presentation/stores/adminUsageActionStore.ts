/**
 * AdminUsageActionStore — auto-generated by .tmp_gen_stores.mjs.
 *
 * Thin store wrapping the Action-side Repository singletons for this feature.
 * Every action delegates to `repo.<method>(...)` inside a try/finally that
 * toggles per-method loading/error state. Follows the §5.4 R5.1 double-export
 * pattern so tests can inject a mock repo via `createAdminUsageActionStore(mock)`.
 *
 * When a feature needs custom orchestration (multi-repo transactions,
 * derived state, cache invalidation) — replace this generated file with a
 * hand-written store; the generator will skip files it did not create.
 */

import { defineStore } from 'pinia'
import { reactive } from 'vue'
import type { AdminUsageRepository } from '@/features/admin-usage/domain/repositories/adminUsageRepository'
import type { ApiKeysRepository } from '@/features/admin-usage/domain/repositories/apiKeysRepository'
import { adminUsageRepository as default_adminUsage_repo } from '@/features/admin-usage/data/repositories/adminUsageRepositoryImpl'
import { apiKeysRepository as default_apiKeys_repo } from '@/features/admin-usage/data/repositories/apiKeysRepositoryImpl'

export function createAdminUsageActionStore(adminUsageRepo: AdminUsageRepository = default_adminUsage_repo, apiKeysRepo: ApiKeysRepository = default_apiKeys_repo) {
  return defineStore('adminUsage/action', () => {
    const loading = reactive<Record<string, boolean>>({ createCleanupTask: false, cancelCleanupTask: false, updateApiKeyGroup: false })
    const errors = reactive<Record<string, unknown>>({ createCleanupTask: null as unknown, cancelCleanupTask: null as unknown, updateApiKeyGroup: null as unknown })

  const createCleanupTask: AdminUsageRepository['createCleanupTask'] = ((...args: unknown[]) => {
    loading.createCleanupTask = true
    errors.createCleanupTask = null
    return Promise.resolve()
      .then(() => (adminUsageRepo.createCleanupTask as (...a: unknown[]) => unknown)(...args))
      .catch((error: unknown) => { errors.createCleanupTask = error; throw error })
      .finally(() => { loading.createCleanupTask = false })
  }) as AdminUsageRepository['createCleanupTask']

  const cancelCleanupTask: AdminUsageRepository['cancelCleanupTask'] = ((...args: unknown[]) => {
    loading.cancelCleanupTask = true
    errors.cancelCleanupTask = null
    return Promise.resolve()
      .then(() => (adminUsageRepo.cancelCleanupTask as (...a: unknown[]) => unknown)(...args))
      .catch((error: unknown) => { errors.cancelCleanupTask = error; throw error })
      .finally(() => { loading.cancelCleanupTask = false })
  }) as AdminUsageRepository['cancelCleanupTask']

  const updateApiKeyGroup: ApiKeysRepository['updateApiKeyGroup'] = ((...args: unknown[]) => {
    loading.updateApiKeyGroup = true
    errors.updateApiKeyGroup = null
    return Promise.resolve()
      .then(() => (apiKeysRepo.updateApiKeyGroup as (...a: unknown[]) => unknown)(...args))
      .catch((error: unknown) => { errors.updateApiKeyGroup = error; throw error })
      .finally(() => { loading.updateApiKeyGroup = false })
  }) as ApiKeysRepository['updateApiKeyGroup']

    return { loading, errors, createCleanupTask, cancelCleanupTask, updateApiKeyGroup }
  })
}

export const useAdminUsageActionStore = createAdminUsageActionStore()
