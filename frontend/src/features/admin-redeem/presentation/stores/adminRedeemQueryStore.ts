/**
 * AdminRedeemQueryStore — auto-generated by .tmp_gen_stores.mjs.
 *
 * Thin store wrapping the Query-side Repository singletons for this feature.
 * Every action delegates to `repo.<method>(...)` inside a try/finally that
 * toggles per-method loading/error state. Follows the §5.4 R5.1 double-export
 * pattern so tests can inject a mock repo via `createAdminRedeemQueryStore(mock)`.
 *
 * When a feature needs custom orchestration (multi-repo transactions,
 * derived state, cache invalidation) — replace this generated file with a
 * hand-written store; the generator will skip files it did not create.
 */

import { defineStore } from 'pinia'
import { reactive } from 'vue'
import type { AdminRedeemRepository } from '@/features/admin-redeem/domain/repositories/adminRedeemRepository'
import { adminRedeemRepository as default_adminRedeem_repo } from '@/features/admin-redeem/data/repositories/adminRedeemRepositoryImpl'

export function createAdminRedeemQueryStore(adminRedeemRepo: AdminRedeemRepository = default_adminRedeem_repo) {
  return defineStore('adminRedeem/query', () => {
    const loading = reactive<Record<string, boolean>>({ list: false, getById: false, getStats: false, exportCodes: false })
    const errors = reactive<Record<string, unknown>>({ list: null as unknown, getById: null as unknown, getStats: null as unknown, exportCodes: null as unknown })

  const list: AdminRedeemRepository['list'] = ((...args: unknown[]) => {
    loading.list = true
    errors.list = null
    return Promise.resolve()
      .then(() => (adminRedeemRepo.list as (...a: unknown[]) => unknown)(...args))
      .catch((error: unknown) => { errors.list = error; throw error })
      .finally(() => { loading.list = false })
  }) as AdminRedeemRepository['list']

  const getById: AdminRedeemRepository['getById'] = ((...args: unknown[]) => {
    loading.getById = true
    errors.getById = null
    return Promise.resolve()
      .then(() => (adminRedeemRepo.getById as (...a: unknown[]) => unknown)(...args))
      .catch((error: unknown) => { errors.getById = error; throw error })
      .finally(() => { loading.getById = false })
  }) as AdminRedeemRepository['getById']

  const getStats: AdminRedeemRepository['getStats'] = ((...args: unknown[]) => {
    loading.getStats = true
    errors.getStats = null
    return Promise.resolve()
      .then(() => (adminRedeemRepo.getStats as (...a: unknown[]) => unknown)(...args))
      .catch((error: unknown) => { errors.getStats = error; throw error })
      .finally(() => { loading.getStats = false })
  }) as AdminRedeemRepository['getStats']

  const exportCodes: AdminRedeemRepository['exportCodes'] = ((...args: unknown[]) => {
    loading.exportCodes = true
    errors.exportCodes = null
    return Promise.resolve()
      .then(() => (adminRedeemRepo.exportCodes as (...a: unknown[]) => unknown)(...args))
      .catch((error: unknown) => { errors.exportCodes = error; throw error })
      .finally(() => { loading.exportCodes = false })
  }) as AdminRedeemRepository['exportCodes']

    return { loading, errors, list, getById, getStats, exportCodes }
  })
}

export const useAdminRedeemQueryStore = createAdminRedeemQueryStore()
